add the following this

down of import discord
add 
import os

//REMOVE THE client.run("Token") Line And Add This


keep_alive()
 
TOKEN = os.environ.get("DISCORD_BOT_SECRET")
 
client.run(TOKEN)



do this all on replit
add a seceret

name it secret and add token in value
